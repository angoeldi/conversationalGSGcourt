{
  "name": "@thecourt/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsup",
    "lint": "echo 'lint (repo-level)';",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "db:migrate": "tsx src/db/migrate.ts",
    "scenarios:validate": "tsx src/scripts/validateScenarios.ts"
  },
  "dependencies": {
    "@thecourt/engine": "workspace:*",
    "@thecourt/shared": "workspace:*",
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "openai": "^4.79.0",
    "pg": "^8.12.0",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
